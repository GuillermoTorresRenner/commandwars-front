# Plantilla de variables de entorno del frontend. Copiar a .env (obligatorio:
# sin VITE_API_URL la app lanza un error al cargar). Debe coincidir con el
# puerto del backend (backend/.env → PORT).

VITE_API_URL=http://localhost:3010
